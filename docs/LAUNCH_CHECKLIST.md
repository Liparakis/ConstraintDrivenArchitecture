# Launch checklist

## Repository metadata

- [x] Confirm the canonical GitHub repository URL: `https://github.com/Liparakis/ConstraintDrivenArchitecture.git`.
- [ ] Add topics: `codex`, `codex-skill`, `agent-skills`, `software-architecture`, `system-design`, `ai-agents`, `llm-evaluation`, `architecture-review`, `java`, `gradle`.
- [ ] Set the description to: `Codex architecture skill that selects complexity from explicit constraints, with a blinded dual-model evaluation harness.`
- [ ] Pin the repository if appropriate, enable private vulnerability reporting, and verify branch protections and Apache-2.0 detection.
- [ ] Verify social preview, unauthenticated installation, README links, and absence of private run data.

Suggested social preview copy is in `assets/social-preview-source.svg`; export it to PNG or JPEG using a reproducible SVG-capable tool before uploading. Do not commit a generated binary unless its production is reproducible.

## Release preparation

- [ ] Choose a version and create an immutable tag only when explicitly approved.
- [ ] Draft release notes, verify installation from the tag, CI, licence, published-evidence hashes, secrets, and README links.
- [ ] Recheck that originality, benchmark, and compatibility claims remain scoped to repository evidence.
