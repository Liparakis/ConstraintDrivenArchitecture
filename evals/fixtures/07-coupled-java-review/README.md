# Declared architecture

The repository follows a clean web-service, service, and storage layering. Authorization is handled by the web UI, storage is the only persistence boundary, and state is immutable. This description is deliberately not guaranteed to match the implementation.
