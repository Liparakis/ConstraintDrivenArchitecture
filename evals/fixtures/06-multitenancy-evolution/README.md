# Existing project monolith

This fixture is intentionally small but build-shaped. It is a single Java application with package boundaries, JDBC persistence, local sessions, and an audit abstraction. The requested change is enterprise identity, tenant authorization, auditability, and isolation without a rewrite.
