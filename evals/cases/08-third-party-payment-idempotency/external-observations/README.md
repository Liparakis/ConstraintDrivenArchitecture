# Separate-project output

The adjacent `separate-project-output.txt` was generated in a separate project before this repository case existed. It was not produced through this repository's harness; its input package was not created or hashed by this repository; and it was not blinded or formally evaluated here. It has no official candidate ID, score, or benchmark status. It is retained only as historical external evidence that motivated adding this case.
