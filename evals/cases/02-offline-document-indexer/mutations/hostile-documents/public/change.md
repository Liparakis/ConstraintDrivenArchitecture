# Constraint amendment

Documents may be actively hostile and parser exploitation is in scope. The application still runs locally and the filesystem remains the source of truth, but parsing must be treated as an adversarial trust boundary.
